还未实现的功能：
6.客户端退出时发送的消息[原本的考虑是在Controller中获取Application的窗口对象，监听其退出事件，但貌似做不到。现在知道可以在
Application中获取Controller对象，因此问题就迎刃而解了。已解决！]

Bug：
1.当用户退出时，如何删除其在I/O流列表中的对象？[答：可能是自动销毁了]
2.如果把接受消息线程中的stop改为interrupt，线程并不会中止，运行会出错。
3.给客户端添加了 发送(Enter) 的功能，并且可以按 Shitf+Enter 换行，但是换行符未处理，暂时不能用这个功能
4.服务端连续点“设置”按钮会出错，客户端连续点“连接”按钮会出错，逻辑上应该是只有第一次点击才有效才对。[已解决]

密码学功能：
1.消息传送时的加解密（OK）
2.消息存储的加解密（OK）
3.用户登录验证界面（OK）
4.数据库用户表用户密钥的安全存储（OK）

PPT怎么做：
1.MCV模式；
2.keytool生成证书；
3.登录验证；
4.抓包；
5.文件加密保存，解密读出。

